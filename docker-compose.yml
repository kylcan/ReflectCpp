services:
  sentinel-api:
    build: .
    ports:
      - "8000:8000"
    environment:
      # Optional: provide either one for LLM-powered mode
      - OPENAI_API_KEY=${OPENAI_API_KEY}
      - GPT5_KEY=${GPT5_KEY}
      - CHATGPT_MODEL=${CHATGPT_MODEL:-gpt-4o}
